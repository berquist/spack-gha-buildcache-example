---
spack:
  view: my_view
  specs:
    - python@3.11

  config:
    install_tree:
      # root: /opt/spack
      padded_length: 128

  mirrors:
    local-buildcache: oci://ghcr.io/berquist/spack-gha-buildcache-example
