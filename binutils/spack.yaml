# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - binutils@=2.42 libs=shared,static
  # - binutils@=2.43 libs=shared,static
  # - binutils@=2.43.1 libs=shared,static
  view: true
  concretizer:
    unify: true
  config:
    install_tree:
      padded_length: 128
